datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Product {
  id          Int     @id
  title       String
  price       Float
  category    String
  from        String
  htmlSnippet String?
  @@unique([id])
}